export default function Avatar(props) {
    
   return(
          <>  
          {/* <img 
            className={props.name}
            // src={`https://i.imgur.com/${props.person.imageId}.jpg`}
            // alt={props.person.fname}
            width={props.size}
            height={props.size}
          />    
       */}
      
       <div name={props.name}>
         </div>
          </>

    );
  }